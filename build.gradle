allprojects {
	group = 'se.shrly'

	repositories {
		mavenCentral()
	}
}

subprojects {
	apply plugin: 'java'

	tasks.withType(Test).configureEach {
		useJUnitPlatform()
		testLogging {
			events "passed", "skipped", "failed"
		}
	}

	tasks.configureEach {
		outputs.cacheIf {
			true
		}
	}
}